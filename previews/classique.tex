%==============================================================
%  STYLE A — Académique classique (Bordeaux + Beige)
%==============================================================
\documentclass[a4paper,11pt]{article}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{setspace}
\setstretch{1.25}
\usepackage{amsmath, amssymb}
\usepackage[dvipsnames]{xcolor}
\usepackage{tikz}
\usepackage[most]{tcolorbox}
\usepackage[framemethod=tikz]{mdframed}
\usepackage[top=2.5cm, bottom=2.5cm, left=3cm, right=2.5cm]{geometry}
\usepackage{enumitem}
\usepackage{titlesec}

%=== PALETTE — Bordeaux & Ivoire =============================
\definecolor{bordeaux}{HTML}{6B1A1A}
\definecolor{ivoirebg}{HTML}{F9F3E8}
\definecolor{ivoire}{HTML}{F0E8D5}
\definecolor{darkgray}{HTML}{2C2C2C}
\definecolor{accentvt}{HTML}{2E5E3C}
\definecolor{accentrd}{HTML}{7A2020}

%=== BOÎTE THÉORÈME ==========================================
\tcbset{
  thmbox/.style={
    enhanced,
    colback=ivoire,
    colframe=bordeaux,
    boxrule=0pt,
    leftrule=4pt,
    arc=0pt,
    left=12pt, right=10pt, top=8pt, bottom=9pt,
    before skip=12pt, after skip=14pt,
    fonttitle=\bfseries\color{bordeaux},
  }
}

%=== BARRES LATÉRALES ========================================
\mdfdefinestyle{proofbar}{
  topline=false, bottomline=false, rightline=false,
  linecolor=bordeaux, linewidth=2.5pt,
  innerleftmargin=14pt, innerrightmargin=0pt,
  innertopmargin=3pt, innerbottommargin=7pt,
  backgroundcolor=white,
  skipabove=4pt, skipbelow=10pt,
}
\mdfdefinestyle{exbar}{
  topline=false, bottomline=false, rightline=false,
  linecolor=accentvt, linewidth=2.5pt,
  innerleftmargin=14pt, innerrightmargin=0pt,
  innertopmargin=3pt, innerbottommargin=7pt,
  backgroundcolor=ivoirebg,
  skipabove=4pt, skipbelow=8pt,
}
\mdfdefinestyle{exobar}{
  topline=false, bottomline=false, rightline=false,
  linecolor=bordeaux!50, linewidth=2pt,
  innerleftmargin=14pt, innerrightmargin=0pt,
  innertopmargin=3pt, innerbottommargin=8pt,
  backgroundcolor=white,
  skipabove=4pt, skipbelow=6pt,
}

%=== TITRE DE LEÇON ==========================================
\newcommand{\lecontitle}[2]{%
  \thispagestyle{empty}
  \vspace*{6pt}
  {\fontsize{22}{28}\selectfont\bfseries\color{darkgray} #1}
  \par\vspace{4pt}
  \noindent\textcolor{bordeaux}{\rule{\textwidth}{1.5pt}}
  \par\vspace{4pt}
  {\large\itshape\color{darkgray!60} #2}
  \par\vspace{20pt}
}

%=== NUMÉRO DE SECTION =======================================
\newcommand{\secnum}[3]{%
  \vspace{18pt}
  \noindent
  {\large\bfseries\color{bordeaux} \S\,#2\enspace}%
  {\large\bfseries\color{darkgray} #3}%
  \par\vspace{2pt}
  \noindent\textcolor{bordeaux!30}{\rule{\textwidth}{0.4pt}}
  \vspace{6pt}
}

%=== RUBRIQ ==================================================
\newcommand{\rubriq}[1]{%
  \vspace{8pt}
  \noindent{\bfseries\color{bordeaux} #1.}\enspace
}

%=== SÉPARATEUR ==============================================
\newcommand{\decsep}{%
  \vspace{12pt}
  \begin{center}
    \textcolor{bordeaux!60}{%
      \rule{30pt}{0.4pt}\quad$\diamond$\quad\rule{30pt}{0.4pt}}
  \end{center}
  \vspace{6pt}
}

%=== PIED DE LEÇON ===========================================
\newcommand{\leconfooter}[1]{%
  \vfill
  \noindent\textcolor{darkgray!25}{\rule{\textwidth}{0.4pt}}
  \begin{center}
    \small\color{darkgray!45}
    Fiche de cours \quad|\quad #1
  \end{center}
}

%=== CHAPITRE (simulé) =======================================
\titleformat{\section}[block]
  {\normalfont\Large\bfseries\color{darkgray}}
  {\textcolor{bordeaux}{\Roman{section}.}}
  {0.7em}{}
  [\vspace{2pt}\textcolor{bordeaux!40}{\titlerule[0.6pt]}]

\setlist[enumerate]{label=\textcolor{bordeaux}{\arabic*.}, itemsep=4pt, topsep=6pt}
\setlist[itemize]{itemsep=4pt, topsep=6pt}

\pagestyle{empty}

\begin{document}

%--------------------------------------------------------------
\lecontitle{Théorème des valeurs intermédiaires}{Analyse réelle — Fonctions continues}

\secnum{bordeaux}{1}{Énoncé}

\begin{tcolorbox}[thmbox]
  \textbf{Théorème (TVI).}\enspace
  Soit $f : [a,b] \to \mathbb{R}$ continue. Pour tout $y$ compris entre
  $f(a)$ et $f(b)$, il existe $c \in [a,b]$ tel que $f(c) = y$.

  \medskip En particulier, si $f(a) < 0 < f(b)$, alors $f$ admet une
  racine dans $]a,b[$.
\end{tcolorbox}

\rubriq{Ingrédient clé}
La preuve repose sur la \textbf{complétude de $\mathbb{R}$} (propriété de
la borne supérieure) et la continuité de $f$.

\decsep

\secnum{bordeaux}{2}{Preuve}

\begin{mdframed}[style=proofbar]
  \textit{Étape 1.} Posons $E = \{x \in [a,b] \mid f(x) \leq y\}$.
  L'ensemble $E$ est non vide (car $a \in E$) et majoré par $b$.

  \medskip
  \textit{Étape 2.} Posons $c = \sup E$. Par continuité de $f$ en $c$,
  $f(c) = y$.\hfill$\blacksquare$
\end{mdframed}

\decsep

\secnum{bordeaux}{3}{Exemples et exercices}

\begin{mdframed}[style=exbar]
  \textbf{Exemple.}\enspace $f(x) = x^3 - 2x - 5$ vérifie $f(2) = -1 < 0$
  et $f(3) = 16 > 0$, donc $f$ admet une racine dans $]2,3[$.
\end{mdframed}

\vspace{6pt}

\begin{mdframed}[style=exobar]
  \begin{enumerate}
    \item Montrer que $e^x = 2-x$ a une solution réelle.
    \item Soit $f$ continue sur $[0,1]$ avec $f(0) = f(1) = 0$.
          Montrer que $f$ n'est pas nécessairement dérivable.
  \end{enumerate}
\end{mdframed}

\leconfooter{B.\ Bolzano (1817) — A.-L.\ Cauchy (1821)}

\end{document}
